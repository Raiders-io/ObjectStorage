meta {
  name: previewObjectFrom
  type: http
  seq: 2
}

get {
  url: {{PROTOCOL}}://{{HOST}}:{{PORT}}/api/v1/storage/users/:userid/objects/preview/:id
  body: none
  auth: inherit
}

params:path {
  userid: [user_id]
  id: [file_name]
}

settings {
  encodeUrl: true
  timeout: 0
}
